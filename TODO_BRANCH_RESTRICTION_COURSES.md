# TODO: تقييد فروع الموظف داخل صفحة Courses.jsx

## الهدف

كل موظف يرى بيانات (الدورات + خيارات الفروع داخل المودال) الخاصة بفروعه فقط عند فتح صفحة `dashboard-electron/src/pages/Courses.jsx`.

## الحالة الآن

- تم إضافة منطق داخل `Courses.jsx`:
  - تحديد `allowedBranchIds` من بيانات المستخدم (branch_ids أو branch_id fallback).
  - فلترة `filteredCourses` بحيث لا تعرض إلا الدورات المرتبطة بفروع المستخدم.
  - فلترة خيارات "كل الفروع" في شريط الفلاتر لتظهر فقط الفروع المسموحة.
  - فلترة قائمة الفروع داخل مودال الإضافة/التعديل.

## المطلوب (Fix)

- يوجد أخطاء TypeScript/Build بسبب تعديلات جزئية في الملف.
- يجب إعادة ضبط الملف ليعود تركيب JSX صحيح بدون أقواس/تنسيقات مكسورة.

## الخطوات القادمة (Fix)

1. إصلاح `dashboard-electron/src/pages/Courses.jsx` حتى يمر `npm run build` بدون أخطاء.
2. بعدها تأكيد أن:
   - غير Admin لا تظهر له دورات خارج فروعه.
   - Admin يرى كل شيء.
   - مودال اختيار الفروع لا يعرض إلا الفروع المسموحة.
