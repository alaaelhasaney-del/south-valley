# ✅ إصلاح زر التحصيل (الصفحتين) - **تم الإصلاح**

## تم الحل:

- ✅ Students.jsx `handleOpenPayment`: tenant_id + filter remaining>0
- ✅ StudentFeesPayment.jsx: tenant_id search + full fees list (no slice(0,1))

## التحقق:

- Buttons work.
- Search finds students.
- Fees dropdown shows all actionable (13 for sample data).

**Status: Complete**
